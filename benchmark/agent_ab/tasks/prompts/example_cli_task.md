# Objective

Ensure CLI setup deprecation messaging behavior is correct and covered by tests.

# Requirements

- Keep existing public CLI command names and behavior stable.
- Only change files required for this specific behavior.
- Do not refactor unrelated CLI components.

# Expected Output

- If needed, update implementation/tests so setup deprecation warning behavior is correct.
- Run the required acceptance command and report result.
